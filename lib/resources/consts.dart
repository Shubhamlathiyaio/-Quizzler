import 'package:flutter/material.dart';

final x  = const Icon(
        Icons.close,
        color: Colors.red,
        size: 30,
      );

      final v = const Icon(
        Icons.check,
        color: Colors.green,
        size: 30,
      );